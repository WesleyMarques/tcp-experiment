This directory contains the coverage information of used versions of the six subject projects, the "sorted_version.txt" file under the main directory of each project provides the sequence of all the versions from latest to oldest(top-down). 
