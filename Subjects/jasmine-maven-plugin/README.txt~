This directory contains the source code of used versions of the six subject projects. The "sorted_version.txt" file under the main directory of each project provides the sequence of all the versions from latest to oldest(top-down). 
